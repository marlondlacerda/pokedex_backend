"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertInfo = exports.convertPath = void 0;
function convertPath(path, srcPrefix, dstPrefix) {
    if (path.startsWith(srcPrefix)) {
        return dstPrefix + path.substring(srcPrefix.length);
    }
    else {
        return path;
    }
}
exports.convertPath = convertPath;
function convertInfo(info, convertPath) {
    let file = info.file;
    if (file) {
        file = convertPath(file);
    }
    if (info.type === 'suite') {
        let children = info.children.map(child => convertInfo(child, convertPath));
        return Object.assign(Object.assign({}, info), { file, children });
    }
    else {
        return Object.assign(Object.assign({}, info), { file });
    }
}
exports.convertInfo = convertInfo;
//# sourceMappingURL=common.js.map